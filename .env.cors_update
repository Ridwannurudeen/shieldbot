
# Add this line to your .env file:
# Replace <YOUR_EXTENSION_ID> with your actual extension ID from chrome://extensions

# For development (allows extension to connect):
CORS_ALLOW_ORIGINS=http://localhost:8000,chrome-extension://<YOUR_EXTENSION_ID>

# Or for easier testing, allow all origins (development only!):
CORS_ALLOW_ORIGINS=*
